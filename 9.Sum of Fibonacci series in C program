#include <stdio.h>
int main() {

  int n,sum=1;
  printf("Enter the number of terms: ");
  scanf("%d", &n);
  for (int i = 2; i<n; i++) {
  	sum=sum+i; 
}
printf("Sum of fibonacci series %d ", sum);
return 0;
}
